# SafeCart Environment Configuration Example

# Server port (fixed to 3000 in AI Studio)
PORT=3000

# Node environment (development / production)
NODE_ENV="development"

# MongoDB Atlas Connection URI
MONGODB_URI="mongodb+srv://ramya:ramya200@cluster0.qjtkz6v.mongodb.net/safecart?retryWrites=true&w=majority&appName=Cluster0"

# JWT Secret for token signing and authentication
JWT_SECRET="safecard_JWT_2026_8FK2XP9MQ7"

# Frontend client URL (for CORS and frontend routing)
CLIENT_URL="http://localhost:5173"

# GEMINI_API_KEY: Optional AI assistant analysis
GEMINI_API_KEY=""

# APP_URL: The URL where this applet is hosted
APP_URL="http://localhost:3000"

